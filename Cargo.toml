[package]
name = "oak-protocol"
version = "0.1.0"
edition = "2021"

[lib]
# Build both a Stylus-compatible cdylib for deployment and an rlib for tests.
crate-type = ["cdylib", "rlib"]

[dependencies]
stylus-sdk = "0.6"
ruint = "=1.16.0"
